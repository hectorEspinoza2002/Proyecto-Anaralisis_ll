import { PassowrdValidatorDirective } from './passowrd-validator.directive';

describe('PassowrdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PassowrdValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
